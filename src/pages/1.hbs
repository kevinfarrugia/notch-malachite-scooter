<div>
  <table>
    <tr>
      <td>
        Data:
      </td>
      <td>
        <strong>{{time}}</strong>
      </td>
    </tr>
  </table>
  <p>
    The timestamp above shows the freshness of the server's response. If you
    refresh the page, this value will be updated with the current server time.
    The
    <i>"Lorem ipsum"</i>
    dummy text below is used to inflate the server's response size.
  </p>
  <p>
    This demo returns a <code>cache-control: no-store</code> response header. This instructs the browser to not cache the response.
  </p>
  <p>
    <img src="/public/learn-performance-caching-1.png" alt="Screenshot of Chrome DevTools showing network response." />
  </p>
  <hr />
  {{{data}}}
</div>